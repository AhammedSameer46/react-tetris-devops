apiVersion: v1
kind: Service

metadata:
  name: react-tetris-service

spec:
  type: {{ .Values.service.type }}

  selector:
    app: react-tetris

  ports:
  - port: {{ .Values.service.port }}
    targetPort: 80